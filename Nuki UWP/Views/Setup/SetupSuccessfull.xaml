<Page
    x:Class="Nuki.Views.Setup.SetupSuccessfull"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nuki.Views.Setup"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Style="{StaticResource SetupPageStyle}">
        <TextBlock  Style="{StaticResource SetupPageTitle}" >
            <Run Text="Glückwunsch!" />
            <LineBreak />
            <LineBreak />
            <Run Text="Dein Schloss wurde erfolgreich eingerichtet!" />
        </TextBlock>
        <Button Style="{StaticResource SetupPageButton}" VerticalAlignment="Bottom" Margin="0 0 0 60" Click="{x:Bind ViewModel.CompleteSetup}">Abschließen</Button>
    </Grid>
</Page>
